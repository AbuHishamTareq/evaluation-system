# API Configuration
# When using Vite proxy (default), this should be empty or the proxy target
# When deploying separately, set to the backend URL
VITE_API_URL=

# Sentry error tracking (optional)
VITE_SENTRY_DSN=
